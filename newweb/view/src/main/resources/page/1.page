


#@lAjax("AJAX加载" "/page/tinyForm.pagelet")#@tButton()AJAX加载#end#end


<script>
    /*layer.open({
            content: '测试AJAX',
            yes: function (index, layero) {
                $.get("/newWeb/page/tinyForm.pagelet",function(str){
                    layer.closeAll();
                    layer.open({
                        type:1,
                        title:"ajax",
                        content: str,
                        maxmin:true,
                        area: ['80%', '70%'],
                        yes: function (index, layero) {
                            layer.confirm('确定关闭？', function (index) {
                                layer.closeAll();
                                layer.prompt({title: '输入信息', formType: 1}, function(text){
                                    layer.closeAll();
                                    layer.msg('确定要删除该记录？', {
                                        icon: 1,
                                        time: 2000, //2秒关闭（如果不配置，默认是3秒）
                                        function(){
                                            layer.msg('只想简单的提示',{icon:1})
                                        }
                                    });
                                });
                            });
                        }
                    });
                });

            }
        });*/
</script>
<script>

    layer.config({shift: 5,icon: 2});
    $(function() {
        /*layer.open({
            content: '测试回调',
            /!*yes: function (index, layero) {
                //do something
                layer.close(index); //如果设定了yes回调，需进行手工关闭
            }*!/
        });*/
        /*layer.confirm('确定要删除该记录？', function (index) {
            layer.close(index);
        });*/
        /*layer.msg('确定要删除该记录？', {
            icon: 1,
            time: 2000 //2秒关闭（如果不配置，默认是3秒）
        });*/
        layer.msg('只想简单的提示',{icon:1})
       /* $.get("/newWeb/page/tinyForm.page",function(str){
            layer.open({
                content: str,
                yes: function (index, layero) {
                    //layer.close(index);
                }
            });
        });*/
        /*layer.prompt({title: '请填写名称', formType: 1}, function(text){
            console.log("test");
        });*/
    })
</script>